リストのインデックスと要素のペアを取得する
==========================================

`zip`を使い整数のリストと組み合わせると、インデックスと要素のペアを取得できます。

```haskell
> xs = ["pen", "pinapple", "apple"]
> zip xs [0..]
[("pen",0),("pinapple",1),("apple",2)]
```
